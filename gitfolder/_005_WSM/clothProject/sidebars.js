const toggleBtn = document.querySelector('navbar_toogleBtn');
const menu = document.querySelector('navbar_toogleBtn');
toggleBtn.addEventListener('click',()=>{
    menu.classList.toggle('active');
});

